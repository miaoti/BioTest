# Phase A
requests>=2.31.0
pylatexenc>=2.10
chromadb>=0.4.22
openai>=1.12.0
tiktoken>=0.6.0

# Phase B
pydantic>=2.6.0
pydantic-settings>=2.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.0
langchain-openai>=0.2.0
langchain-google-genai>=2.0.0
langchain-anthropic>=0.3.0

# Phase C / D — Hypothesis property testing + HypoFuzz coverage feedback
# (Rank 7 lever). HypoFuzz is the Hypothesis-author-maintained companion
# that plugs branch-coverage signals directly into Hypothesis strategy
# choices, going beyond `hypothesis.target()`'s scalar objective. Wraps
# our existing @given path; no per-SUT code needed.
hypothesis>=6.100
hypofuzz>=25.09

# Dev: static NameError detection in biotest.py's Phase D branches.
# Catches undefined-name bugs that survive the unit suite because they
# only fire inside rarely-entered feedback-loop code paths.
pyflakes>=3.0

# Phase C SUTs
# ------------
# vcfpy (bihealth): pure-Python VCF parser, independent reimplementation
# (NOT a wrapper over htslib). Drop-in coverage via coverage.py. Pinned
# loosely to the 0.14.x line — v0.14.2 (2025-10-02) is the baseline.
vcfpy>=0.14.2,<0.15
# Note: noodles-vcf is a Rust crate pulled by Cargo when you build the
# harness in harnesses/rust/noodles_harness/. It is NOT a Python
# dependency and does not appear here — see that directory's README.
