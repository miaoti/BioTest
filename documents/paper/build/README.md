# build/

Auto-generated LaTeX build artifacts (`.aux`, `.bbl`, `.blg`, `.log`,
`.out`, `texput.log`). Safe to delete; they are regenerated by the
`pdflatex` / `bibtex` cycle on the next build.

The output PDF (`main.pdf`) is kept at the paper root, not here.
