
from inspect import signature as _mutmut_signature

def _mutmut_trampoline(orig, mutants, *args, **kwargs):
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from __main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from __main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        return orig(*args, **kwargs)
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        return orig(*args, **kwargs)
    mutant_name = mutant_under_test.rpartition('.')[-1]
    return mutants[mutant_name](*args, **kwargs)


"""Support for using reading tabix files and using them in bgzip files."""

import dataclasses
import enum
import gzip
import pathlib
import struct

from vcfpy.bgzf import BgzfReader


class FileFormat(enum.Enum):
    """Enum for file formats supported by tabix."""

    #: Generic tabix file.
    GENERIC = 0
    #: SAM file.
    SAM = 1
    #: VCF file.
    VCF = 2


@dataclasses.dataclass
class Chunk:
    """Chunk."""

    #: Begin virtual offset.
    beg: int
    #: End virtual offset.
    end: int


@dataclasses.dataclass
class Bin:
    """Bin with chunks."""

    #: Bin number.
    number: int
    #: Chunks in this bin.
    chunks: list[Chunk]


@dataclasses.dataclass
class SequenceIndex:
    """Per-sequence index."""

    #: Bins containing chunks.
    bins: list[Bin]
    #: Linear index intervals.
    offsets: list[int]


@dataclasses.dataclass
class TabixIndex:
    """Index as read from Tabix files and relevant after reading the index."""

    #: Format of underlying file.
    format: FileFormat
    #: Column for sequence name.
    col_seq: int
    #: Column for begin position.
    col_beg: int
    #: Column for end position.
    col_end: int
    #: Meta character.
    meta: bytes
    #: Lines to skip at the beginning.
    skip: int
    #: Per-sequence indices.
    indices: dict[str, SequenceIndex]
    #: Optional number of unmapped reads.
    num_no_coord: int | None = None


def reg2bins__mutmut_orig(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_1(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = None
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_2(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end += 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_3(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end = 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_4(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 2  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_5(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 1
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_6(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = None
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_7(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 15 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_8(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 - (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_9(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (6 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_10(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 >> 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_11(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 2) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_12(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) - 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_13(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 6  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_14(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = None  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_15(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(6 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_16(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 - 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_17(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 2):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_18(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t - (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_19(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg << s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_20(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = None
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_21(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t - (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_22(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end << s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_23(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = None
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_24(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(None, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_25(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e - 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_26(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 2):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_27(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range( e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_28(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(None)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_29(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t -= 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_30(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t = 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_31(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 2 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_32(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 >> ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_33(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level >> 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_34(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 2) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_35(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) - level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_36(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s += 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_37(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s = 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_38(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 4  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(bins))


def reg2bins__mutmut_39(beg: int, end: int) -> list[int]:
    """Get list of bins that may overlap a region [beg, end).

    Based on the UCSC binning scheme used by tabix.

    :param beg: 0-based start position (inclusive)
    :param end: 0-based end position (exclusive)
    :return: list of bin numbers that may overlap the region (in reverse order)
    """
    bins = []
    end -= 1  # Convert to exclusive end to inclusive end

    # UCSC binning scheme with 5 levels and min_shift=14
    t = 0
    s = 14 + (5 << 1) + 5  # min_shift + (n_levels << 1) + n_levels = 14 + 10 + 5 = 29

    for level in range(5 + 1):  # n_levels + 1 = 6 levels (0-5)
        b = t + (beg >> s)
        e = t + (end >> s)
        for k in range(b, e + 1):
            bins.append(k)
        t += 1 << ((level << 1) + level)  # Update t for next level
        s -= 3  # Decrease shift by 3 for next level

    # Return in reverse order as per legacy implementation
    return list(reversed(None))

reg2bins__mutmut_mutants = {
'reg2bins__mutmut_1': reg2bins__mutmut_1, 
    'reg2bins__mutmut_2': reg2bins__mutmut_2, 
    'reg2bins__mutmut_3': reg2bins__mutmut_3, 
    'reg2bins__mutmut_4': reg2bins__mutmut_4, 
    'reg2bins__mutmut_5': reg2bins__mutmut_5, 
    'reg2bins__mutmut_6': reg2bins__mutmut_6, 
    'reg2bins__mutmut_7': reg2bins__mutmut_7, 
    'reg2bins__mutmut_8': reg2bins__mutmut_8, 
    'reg2bins__mutmut_9': reg2bins__mutmut_9, 
    'reg2bins__mutmut_10': reg2bins__mutmut_10, 
    'reg2bins__mutmut_11': reg2bins__mutmut_11, 
    'reg2bins__mutmut_12': reg2bins__mutmut_12, 
    'reg2bins__mutmut_13': reg2bins__mutmut_13, 
    'reg2bins__mutmut_14': reg2bins__mutmut_14, 
    'reg2bins__mutmut_15': reg2bins__mutmut_15, 
    'reg2bins__mutmut_16': reg2bins__mutmut_16, 
    'reg2bins__mutmut_17': reg2bins__mutmut_17, 
    'reg2bins__mutmut_18': reg2bins__mutmut_18, 
    'reg2bins__mutmut_19': reg2bins__mutmut_19, 
    'reg2bins__mutmut_20': reg2bins__mutmut_20, 
    'reg2bins__mutmut_21': reg2bins__mutmut_21, 
    'reg2bins__mutmut_22': reg2bins__mutmut_22, 
    'reg2bins__mutmut_23': reg2bins__mutmut_23, 
    'reg2bins__mutmut_24': reg2bins__mutmut_24, 
    'reg2bins__mutmut_25': reg2bins__mutmut_25, 
    'reg2bins__mutmut_26': reg2bins__mutmut_26, 
    'reg2bins__mutmut_27': reg2bins__mutmut_27, 
    'reg2bins__mutmut_28': reg2bins__mutmut_28, 
    'reg2bins__mutmut_29': reg2bins__mutmut_29, 
    'reg2bins__mutmut_30': reg2bins__mutmut_30, 
    'reg2bins__mutmut_31': reg2bins__mutmut_31, 
    'reg2bins__mutmut_32': reg2bins__mutmut_32, 
    'reg2bins__mutmut_33': reg2bins__mutmut_33, 
    'reg2bins__mutmut_34': reg2bins__mutmut_34, 
    'reg2bins__mutmut_35': reg2bins__mutmut_35, 
    'reg2bins__mutmut_36': reg2bins__mutmut_36, 
    'reg2bins__mutmut_37': reg2bins__mutmut_37, 
    'reg2bins__mutmut_38': reg2bins__mutmut_38, 
    'reg2bins__mutmut_39': reg2bins__mutmut_39
}

def reg2bins(*args, **kwargs):
    return _mutmut_trampoline(reg2bins__mutmut_orig, reg2bins__mutmut_mutants, *args, **kwargs) 

reg2bins.__signature__ = _mutmut_signature(reg2bins__mutmut_orig)
reg2bins__mutmut_orig.__name__ = 'reg2bins'




def read_index__mutmut_orig(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_1(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(None, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_2(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "XXrbXX") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_3(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open( "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_4(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(5)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_5(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = None
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_6(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic == b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_7(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"XXTBI\x01XX":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_8(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("XX<iXX", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_9(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(5))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_10(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[1]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_11(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[None]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_12(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("XX<iXX", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_13(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(5))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_14(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[1]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_15(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[None]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_16(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = None
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_17(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("XX<iXX", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_18(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(5))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_19(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[1]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_20(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[None]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_21(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = None
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_22(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("XX<iXX", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_23(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(5))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_24(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[1]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_25(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[None]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_26(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = None
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_27(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("XX<iXX", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_28(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(5))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_29(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[1]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_30(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[None]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_31(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = None
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_32(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("XX<iXX", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_33(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(5))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_34(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[1]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_35(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[None]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_36(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = None
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_37(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("XX<iXX", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_38(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(5))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_39(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[1]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_40(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[None]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_41(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = None
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_42(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("XX<iXX", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_43(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(5))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_44(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[1]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_45(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[None]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_46(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = None

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_47(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(None)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_48(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = None
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_49(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"XX\x00XX").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_50(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"XX\x00XX")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_51(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = None
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_52(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("XXutf-8XX") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_53(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = None

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_54(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = None

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_55(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("XX<iXX", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_56(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(5))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_57(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[1]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_58(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[None]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_59(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = None

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_60(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = None
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_61(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(None):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_62(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("XX<IXX", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_63(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(5))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_64(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[1]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_65(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[None]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_66(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = None
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_67(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("XX<iXX", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_68(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(5))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_69(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[1]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_70(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[None]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_71(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = None

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_72(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = None
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_73(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(None):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_74(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("XX<QXX", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_75(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(9))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_76(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[1]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_77(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[None]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_78(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = None
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_79(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("XX<QXX", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_80(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(9))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_81(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[1]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_82(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[None]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_83(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = None
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_84(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=None, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_85(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=None))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_86(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk( end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_87(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg,))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_88(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=None, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_89(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=None))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_90(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin( chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_91(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number,))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_92(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("XX<iXX", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_93(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(5))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_94(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[1]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_95(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[None]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_96(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = None
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_97(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = None
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_98(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(None):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_99(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("XX<QXX", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_100(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(9))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_101(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[1]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_102(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[None]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_103(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = None
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_104(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(None)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_105(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[None] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_106(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=None, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_107(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=None)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_108(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex( offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_109(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins,)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_110(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = None

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_111(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = ""
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_112(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(9)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_113(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = None
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_114(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) != 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_115(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 9:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_116(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("XX<QXX", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_117(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", None)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_118(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q",)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_119(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[1]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_120(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[None]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_121(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = None
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_122(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val | 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_123(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 65536)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_124(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = None  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_125(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=None,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_126(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=None,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_127(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=None,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_128(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=None,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_129(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(None).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_130(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("XXutf-8XX"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_131(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=None,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_132(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=None,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_133(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=None,
        )


def read_index__mutmut_134(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_135(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_136(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_137(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_138(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            skip=skip,
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_139(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            indices=indices,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_140(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            num_no_coord=num_no_coord,
        )


def read_index__mutmut_141(path_tbi: pathlib.Path | str) -> TabixIndex:
    """Read tabix index from given path.

    :param path_tbi: path to the tabix index file
    :return: the read index
    """
    with gzip.open(path_tbi, "rb") as f:
        # Read header
        magic = f.read(4)
        if magic != b"TBI\x01":
            raise ValueError(f"Invalid tabix magic: {magic}")

        # Read configuration
        struct.unpack("<i", f.read(4))[0]
        format_val = struct.unpack("<i", f.read(4))[0]
        col_seq = struct.unpack("<i", f.read(4))[0]
        col_beg = struct.unpack("<i", f.read(4))[0]
        col_end = struct.unpack("<i", f.read(4))[0]
        meta = struct.unpack("<i", f.read(4))[0]
        skip = struct.unpack("<i", f.read(4))[0]
        l_nm = struct.unpack("<i", f.read(4))[0]

        # Read sequence names
        names_data = f.read(l_nm)
        names = names_data.rstrip(b"\x00").split(b"\x00")
        sequence_names = [name.decode("utf-8") for name in names if name]

        # Create indices dictionary
        indices = {}

        # Read per-sequence indices
        for seq_name in sequence_names:
            # Read number of bins
            n_bin = struct.unpack("<i", f.read(4))[0]

            bins = []
            # Read bins
            for _ in range(n_bin):
                bin_number = struct.unpack("<I", f.read(4))[0]
                n_chunk = struct.unpack("<i", f.read(4))[0]

                # Read chunks for this bin
                chunks = []
                for _ in range(n_chunk):
                    cnk_beg = struct.unpack("<Q", f.read(8))[0]
                    cnk_end = struct.unpack("<Q", f.read(8))[0]
                    chunks.append(Chunk(beg=cnk_beg, end=cnk_end))

                bins.append(Bin(number=bin_number, chunks=chunks))

            # Read linear index
            n_intv = struct.unpack("<i", f.read(4))[0]
            offsets = []
            for _ in range(n_intv):
                ioff = struct.unpack("<Q", f.read(8))[0]
                offsets.append(ioff)

            indices[seq_name] = SequenceIndex(bins=bins, offsets=offsets)

        # Try to read optional unmapped reads count
        num_no_coord = None
        try:
            remaining = f.read(8)
            if len(remaining) == 8:
                num_no_coord = struct.unpack("<Q", remaining)[0]
        except (IOError, struct.error):  # pragma: no cover
            pass  # Optional field, ignore if not present

        # Convert format value to enum
        format_enum = FileFormat(format_val & 0xFFFF)  # Mask off coordinate type flag

        return TabixIndex(
            format=format_enum,
            col_seq=col_seq,
            col_beg=col_beg,
            col_end=col_end,
            meta=chr(meta).encode("utf-8"),
            skip=skip,
            indices=indices,
        )

read_index__mutmut_mutants = {
'read_index__mutmut_1': read_index__mutmut_1, 
    'read_index__mutmut_2': read_index__mutmut_2, 
    'read_index__mutmut_3': read_index__mutmut_3, 
    'read_index__mutmut_4': read_index__mutmut_4, 
    'read_index__mutmut_5': read_index__mutmut_5, 
    'read_index__mutmut_6': read_index__mutmut_6, 
    'read_index__mutmut_7': read_index__mutmut_7, 
    'read_index__mutmut_8': read_index__mutmut_8, 
    'read_index__mutmut_9': read_index__mutmut_9, 
    'read_index__mutmut_10': read_index__mutmut_10, 
    'read_index__mutmut_11': read_index__mutmut_11, 
    'read_index__mutmut_12': read_index__mutmut_12, 
    'read_index__mutmut_13': read_index__mutmut_13, 
    'read_index__mutmut_14': read_index__mutmut_14, 
    'read_index__mutmut_15': read_index__mutmut_15, 
    'read_index__mutmut_16': read_index__mutmut_16, 
    'read_index__mutmut_17': read_index__mutmut_17, 
    'read_index__mutmut_18': read_index__mutmut_18, 
    'read_index__mutmut_19': read_index__mutmut_19, 
    'read_index__mutmut_20': read_index__mutmut_20, 
    'read_index__mutmut_21': read_index__mutmut_21, 
    'read_index__mutmut_22': read_index__mutmut_22, 
    'read_index__mutmut_23': read_index__mutmut_23, 
    'read_index__mutmut_24': read_index__mutmut_24, 
    'read_index__mutmut_25': read_index__mutmut_25, 
    'read_index__mutmut_26': read_index__mutmut_26, 
    'read_index__mutmut_27': read_index__mutmut_27, 
    'read_index__mutmut_28': read_index__mutmut_28, 
    'read_index__mutmut_29': read_index__mutmut_29, 
    'read_index__mutmut_30': read_index__mutmut_30, 
    'read_index__mutmut_31': read_index__mutmut_31, 
    'read_index__mutmut_32': read_index__mutmut_32, 
    'read_index__mutmut_33': read_index__mutmut_33, 
    'read_index__mutmut_34': read_index__mutmut_34, 
    'read_index__mutmut_35': read_index__mutmut_35, 
    'read_index__mutmut_36': read_index__mutmut_36, 
    'read_index__mutmut_37': read_index__mutmut_37, 
    'read_index__mutmut_38': read_index__mutmut_38, 
    'read_index__mutmut_39': read_index__mutmut_39, 
    'read_index__mutmut_40': read_index__mutmut_40, 
    'read_index__mutmut_41': read_index__mutmut_41, 
    'read_index__mutmut_42': read_index__mutmut_42, 
    'read_index__mutmut_43': read_index__mutmut_43, 
    'read_index__mutmut_44': read_index__mutmut_44, 
    'read_index__mutmut_45': read_index__mutmut_45, 
    'read_index__mutmut_46': read_index__mutmut_46, 
    'read_index__mutmut_47': read_index__mutmut_47, 
    'read_index__mutmut_48': read_index__mutmut_48, 
    'read_index__mutmut_49': read_index__mutmut_49, 
    'read_index__mutmut_50': read_index__mutmut_50, 
    'read_index__mutmut_51': read_index__mutmut_51, 
    'read_index__mutmut_52': read_index__mutmut_52, 
    'read_index__mutmut_53': read_index__mutmut_53, 
    'read_index__mutmut_54': read_index__mutmut_54, 
    'read_index__mutmut_55': read_index__mutmut_55, 
    'read_index__mutmut_56': read_index__mutmut_56, 
    'read_index__mutmut_57': read_index__mutmut_57, 
    'read_index__mutmut_58': read_index__mutmut_58, 
    'read_index__mutmut_59': read_index__mutmut_59, 
    'read_index__mutmut_60': read_index__mutmut_60, 
    'read_index__mutmut_61': read_index__mutmut_61, 
    'read_index__mutmut_62': read_index__mutmut_62, 
    'read_index__mutmut_63': read_index__mutmut_63, 
    'read_index__mutmut_64': read_index__mutmut_64, 
    'read_index__mutmut_65': read_index__mutmut_65, 
    'read_index__mutmut_66': read_index__mutmut_66, 
    'read_index__mutmut_67': read_index__mutmut_67, 
    'read_index__mutmut_68': read_index__mutmut_68, 
    'read_index__mutmut_69': read_index__mutmut_69, 
    'read_index__mutmut_70': read_index__mutmut_70, 
    'read_index__mutmut_71': read_index__mutmut_71, 
    'read_index__mutmut_72': read_index__mutmut_72, 
    'read_index__mutmut_73': read_index__mutmut_73, 
    'read_index__mutmut_74': read_index__mutmut_74, 
    'read_index__mutmut_75': read_index__mutmut_75, 
    'read_index__mutmut_76': read_index__mutmut_76, 
    'read_index__mutmut_77': read_index__mutmut_77, 
    'read_index__mutmut_78': read_index__mutmut_78, 
    'read_index__mutmut_79': read_index__mutmut_79, 
    'read_index__mutmut_80': read_index__mutmut_80, 
    'read_index__mutmut_81': read_index__mutmut_81, 
    'read_index__mutmut_82': read_index__mutmut_82, 
    'read_index__mutmut_83': read_index__mutmut_83, 
    'read_index__mutmut_84': read_index__mutmut_84, 
    'read_index__mutmut_85': read_index__mutmut_85, 
    'read_index__mutmut_86': read_index__mutmut_86, 
    'read_index__mutmut_87': read_index__mutmut_87, 
    'read_index__mutmut_88': read_index__mutmut_88, 
    'read_index__mutmut_89': read_index__mutmut_89, 
    'read_index__mutmut_90': read_index__mutmut_90, 
    'read_index__mutmut_91': read_index__mutmut_91, 
    'read_index__mutmut_92': read_index__mutmut_92, 
    'read_index__mutmut_93': read_index__mutmut_93, 
    'read_index__mutmut_94': read_index__mutmut_94, 
    'read_index__mutmut_95': read_index__mutmut_95, 
    'read_index__mutmut_96': read_index__mutmut_96, 
    'read_index__mutmut_97': read_index__mutmut_97, 
    'read_index__mutmut_98': read_index__mutmut_98, 
    'read_index__mutmut_99': read_index__mutmut_99, 
    'read_index__mutmut_100': read_index__mutmut_100, 
    'read_index__mutmut_101': read_index__mutmut_101, 
    'read_index__mutmut_102': read_index__mutmut_102, 
    'read_index__mutmut_103': read_index__mutmut_103, 
    'read_index__mutmut_104': read_index__mutmut_104, 
    'read_index__mutmut_105': read_index__mutmut_105, 
    'read_index__mutmut_106': read_index__mutmut_106, 
    'read_index__mutmut_107': read_index__mutmut_107, 
    'read_index__mutmut_108': read_index__mutmut_108, 
    'read_index__mutmut_109': read_index__mutmut_109, 
    'read_index__mutmut_110': read_index__mutmut_110, 
    'read_index__mutmut_111': read_index__mutmut_111, 
    'read_index__mutmut_112': read_index__mutmut_112, 
    'read_index__mutmut_113': read_index__mutmut_113, 
    'read_index__mutmut_114': read_index__mutmut_114, 
    'read_index__mutmut_115': read_index__mutmut_115, 
    'read_index__mutmut_116': read_index__mutmut_116, 
    'read_index__mutmut_117': read_index__mutmut_117, 
    'read_index__mutmut_118': read_index__mutmut_118, 
    'read_index__mutmut_119': read_index__mutmut_119, 
    'read_index__mutmut_120': read_index__mutmut_120, 
    'read_index__mutmut_121': read_index__mutmut_121, 
    'read_index__mutmut_122': read_index__mutmut_122, 
    'read_index__mutmut_123': read_index__mutmut_123, 
    'read_index__mutmut_124': read_index__mutmut_124, 
    'read_index__mutmut_125': read_index__mutmut_125, 
    'read_index__mutmut_126': read_index__mutmut_126, 
    'read_index__mutmut_127': read_index__mutmut_127, 
    'read_index__mutmut_128': read_index__mutmut_128, 
    'read_index__mutmut_129': read_index__mutmut_129, 
    'read_index__mutmut_130': read_index__mutmut_130, 
    'read_index__mutmut_131': read_index__mutmut_131, 
    'read_index__mutmut_132': read_index__mutmut_132, 
    'read_index__mutmut_133': read_index__mutmut_133, 
    'read_index__mutmut_134': read_index__mutmut_134, 
    'read_index__mutmut_135': read_index__mutmut_135, 
    'read_index__mutmut_136': read_index__mutmut_136, 
    'read_index__mutmut_137': read_index__mutmut_137, 
    'read_index__mutmut_138': read_index__mutmut_138, 
    'read_index__mutmut_139': read_index__mutmut_139, 
    'read_index__mutmut_140': read_index__mutmut_140, 
    'read_index__mutmut_141': read_index__mutmut_141
}

def read_index(*args, **kwargs):
    return _mutmut_trampoline(read_index__mutmut_orig, read_index__mutmut_mutants, *args, **kwargs) 

read_index.__signature__ = _mutmut_signature(read_index__mutmut_orig)
read_index__mutmut_orig.__name__ = 'read_index'




class TabixFile:
    """Provides easy access for reading tabix files."""

    def xǁTabixFileǁ__init____mutmut_orig(self, *, filename: pathlib.Path | str, index: pathlib.Path | str | None = None):
        """Create new ``TabixFile``.

        :param filename: path to the bgzip-ed file
        :param index: path to the tabix index file
        """

        self.filename: str = str(filename)
        self.index_filename: str = f"{filename}.tbi" if index is None else str(index)
        self.closed: bool = False
        self.file: BgzfReader = BgzfReader(self.filename)
        self.index_file: TabixIndex = read_index(self.index_filename)

    def xǁTabixFileǁ__init____mutmut_1(self, *, filename: pathlib.Path | str, index: pathlib.Path | str | None = None):
        """Create new ``TabixFile``.

        :param filename: path to the bgzip-ed file
        :param index: path to the tabix index file
        """

        self.filename: str = str(None)
        self.index_filename: str = f"{filename}.tbi" if index is None else str(index)
        self.closed: bool = False
        self.file: BgzfReader = BgzfReader(self.filename)
        self.index_file: TabixIndex = read_index(self.index_filename)

    def xǁTabixFileǁ__init____mutmut_2(self, *, filename: pathlib.Path | str, index: pathlib.Path | str | None = None):
        """Create new ``TabixFile``.

        :param filename: path to the bgzip-ed file
        :param index: path to the tabix index file
        """

        self.filename: str = None
        self.index_filename: str = f"{filename}.tbi" if index is None else str(index)
        self.closed: bool = False
        self.file: BgzfReader = BgzfReader(self.filename)
        self.index_file: TabixIndex = read_index(self.index_filename)

    def xǁTabixFileǁ__init____mutmut_3(self, *, filename: pathlib.Path | str, index: pathlib.Path | str | None = None):
        """Create new ``TabixFile``.

        :param filename: path to the bgzip-ed file
        :param index: path to the tabix index file
        """

        self.filename: str = str(filename)
        self.index_filename: str = f"{filename}.tbi" if index is not None else str(index)
        self.closed: bool = False
        self.file: BgzfReader = BgzfReader(self.filename)
        self.index_file: TabixIndex = read_index(self.index_filename)

    def xǁTabixFileǁ__init____mutmut_4(self, *, filename: pathlib.Path | str, index: pathlib.Path | str | None = None):
        """Create new ``TabixFile``.

        :param filename: path to the bgzip-ed file
        :param index: path to the tabix index file
        """

        self.filename: str = str(filename)
        self.index_filename: str = f"{filename}.tbi" if index is None else str(None)
        self.closed: bool = False
        self.file: BgzfReader = BgzfReader(self.filename)
        self.index_file: TabixIndex = read_index(self.index_filename)

    def xǁTabixFileǁ__init____mutmut_5(self, *, filename: pathlib.Path | str, index: pathlib.Path | str | None = None):
        """Create new ``TabixFile``.

        :param filename: path to the bgzip-ed file
        :param index: path to the tabix index file
        """

        self.filename: str = str(filename)
        self.index_filename: str = None
        self.closed: bool = False
        self.file: BgzfReader = BgzfReader(self.filename)
        self.index_file: TabixIndex = read_index(self.index_filename)

    def xǁTabixFileǁ__init____mutmut_6(self, *, filename: pathlib.Path | str, index: pathlib.Path | str | None = None):
        """Create new ``TabixFile``.

        :param filename: path to the bgzip-ed file
        :param index: path to the tabix index file
        """

        self.filename: str = str(filename)
        self.index_filename: str = f"{filename}.tbi" if index is None else str(index)
        self.closed: bool = True
        self.file: BgzfReader = BgzfReader(self.filename)
        self.index_file: TabixIndex = read_index(self.index_filename)

    def xǁTabixFileǁ__init____mutmut_7(self, *, filename: pathlib.Path | str, index: pathlib.Path | str | None = None):
        """Create new ``TabixFile``.

        :param filename: path to the bgzip-ed file
        :param index: path to the tabix index file
        """

        self.filename: str = str(filename)
        self.index_filename: str = f"{filename}.tbi" if index is None else str(index)
        self.closed: bool = None
        self.file: BgzfReader = BgzfReader(self.filename)
        self.index_file: TabixIndex = read_index(self.index_filename)

    def xǁTabixFileǁ__init____mutmut_8(self, *, filename: pathlib.Path | str, index: pathlib.Path | str | None = None):
        """Create new ``TabixFile``.

        :param filename: path to the bgzip-ed file
        :param index: path to the tabix index file
        """

        self.filename: str = str(filename)
        self.index_filename: str = f"{filename}.tbi" if index is None else str(index)
        self.closed: bool = False
        self.file: BgzfReader = None
        self.index_file: TabixIndex = read_index(self.index_filename)

    def xǁTabixFileǁ__init____mutmut_9(self, *, filename: pathlib.Path | str, index: pathlib.Path | str | None = None):
        """Create new ``TabixFile``.

        :param filename: path to the bgzip-ed file
        :param index: path to the tabix index file
        """

        self.filename: str = str(filename)
        self.index_filename: str = f"{filename}.tbi" if index is None else str(index)
        self.closed: bool = False
        self.file: BgzfReader = BgzfReader(self.filename)
        self.index_file: TabixIndex = None

    xǁTabixFileǁ__init____mutmut_mutants = {
    'xǁTabixFileǁ__init____mutmut_1': xǁTabixFileǁ__init____mutmut_1, 
        'xǁTabixFileǁ__init____mutmut_2': xǁTabixFileǁ__init____mutmut_2, 
        'xǁTabixFileǁ__init____mutmut_3': xǁTabixFileǁ__init____mutmut_3, 
        'xǁTabixFileǁ__init____mutmut_4': xǁTabixFileǁ__init____mutmut_4, 
        'xǁTabixFileǁ__init____mutmut_5': xǁTabixFileǁ__init____mutmut_5, 
        'xǁTabixFileǁ__init____mutmut_6': xǁTabixFileǁ__init____mutmut_6, 
        'xǁTabixFileǁ__init____mutmut_7': xǁTabixFileǁ__init____mutmut_7, 
        'xǁTabixFileǁ__init____mutmut_8': xǁTabixFileǁ__init____mutmut_8, 
        'xǁTabixFileǁ__init____mutmut_9': xǁTabixFileǁ__init____mutmut_9
    }

    def __init__(self, *args, **kwargs):
        return _mutmut_trampoline(object.__getattribute__(self, "xǁTabixFileǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁTabixFileǁ__init____mutmut_mutants"), *args, **kwargs) 

    __init__.__signature__ = _mutmut_signature(xǁTabixFileǁ__init____mutmut_orig)
    xǁTabixFileǁ__init____mutmut_orig.__name__ = 'xǁTabixFileǁ__init__'



    def xǁTabixFileǁfetch__mutmut_orig(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_1(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is  None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_2(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region or reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_3(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("XXCannot specify both region and reference/start/endXX")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_4(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is  None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_5(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is  None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_6(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None and end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_7(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is not None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_8(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) or reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_9(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("XXIf start or end is given, reference must be given tooXX")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_10(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if "XX:XX" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_11(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" not in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_12(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split("XX:XX", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_13(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 2)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_14(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = None
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_15(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "XX-XX" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_16(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" not in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_17(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("XX-XX", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_18(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 2)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_19(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = None
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_20(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace("XX,XX", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_21(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", "XXXX"))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_22(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = None
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_23(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace("XX,XX", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_24(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", "XXXX"))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_25(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = None
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_26(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace("XX,XX", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_27(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", "XXXX"))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_28(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = None
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_29(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = ""
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_30(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = None
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_31(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = None
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_32(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = ""
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_33(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = ""
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_34(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is  None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_35(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference  in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_36(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=None,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_37(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=None,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_38(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=None,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_39(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            reference=reference,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_40(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            start=start,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_41(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            end=end,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_42(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            bgzf_file=self.file,
        )

    def xǁTabixFileǁfetch__mutmut_43(
        self,
        *,
        reference: str | None = None,
        start: int | None = None,
        end: int | None = None,
        region: str | None = None,
    ) -> "TabixFileIter":
        """Fetch iterator for given region."""
        if region and reference is not None:
            raise ValueError("Cannot specify both region and reference/start/end")
        if (start is not None or end is not None) and reference is None:
            raise ValueError("If start or end is given, reference must be given too")

        # Parse out the region, can be one of "sequence" or "sequence:start-end",
        # commas are stripped from start and end.
        if region:
            if ":" in region:
                ref, pos = region.split(":", 1)
                if "-" in pos:
                    start_str, end_str = pos.split("-", 1)
                    start = int(start_str.replace(",", ""))
                    end = int(end_str.replace(",", ""))
                else:
                    start = int(pos.replace(",", ""))
                    end = None
                reference = ref
            else:
                reference = region
                start = None
                end = None
        assert reference is not None

        # Validate reference.
        if reference not in self.index_file.indices:
            raise ValueError(f"Reference {reference} not found in index")

        # Construct and return the iterator
        return TabixFileIter(
            index=self.index_file,
            reference=reference,
            start=start,
            end=end,
        )

    xǁTabixFileǁfetch__mutmut_mutants = {
    'xǁTabixFileǁfetch__mutmut_1': xǁTabixFileǁfetch__mutmut_1, 
        'xǁTabixFileǁfetch__mutmut_2': xǁTabixFileǁfetch__mutmut_2, 
        'xǁTabixFileǁfetch__mutmut_3': xǁTabixFileǁfetch__mutmut_3, 
        'xǁTabixFileǁfetch__mutmut_4': xǁTabixFileǁfetch__mutmut_4, 
        'xǁTabixFileǁfetch__mutmut_5': xǁTabixFileǁfetch__mutmut_5, 
        'xǁTabixFileǁfetch__mutmut_6': xǁTabixFileǁfetch__mutmut_6, 
        'xǁTabixFileǁfetch__mutmut_7': xǁTabixFileǁfetch__mutmut_7, 
        'xǁTabixFileǁfetch__mutmut_8': xǁTabixFileǁfetch__mutmut_8, 
        'xǁTabixFileǁfetch__mutmut_9': xǁTabixFileǁfetch__mutmut_9, 
        'xǁTabixFileǁfetch__mutmut_10': xǁTabixFileǁfetch__mutmut_10, 
        'xǁTabixFileǁfetch__mutmut_11': xǁTabixFileǁfetch__mutmut_11, 
        'xǁTabixFileǁfetch__mutmut_12': xǁTabixFileǁfetch__mutmut_12, 
        'xǁTabixFileǁfetch__mutmut_13': xǁTabixFileǁfetch__mutmut_13, 
        'xǁTabixFileǁfetch__mutmut_14': xǁTabixFileǁfetch__mutmut_14, 
        'xǁTabixFileǁfetch__mutmut_15': xǁTabixFileǁfetch__mutmut_15, 
        'xǁTabixFileǁfetch__mutmut_16': xǁTabixFileǁfetch__mutmut_16, 
        'xǁTabixFileǁfetch__mutmut_17': xǁTabixFileǁfetch__mutmut_17, 
        'xǁTabixFileǁfetch__mutmut_18': xǁTabixFileǁfetch__mutmut_18, 
        'xǁTabixFileǁfetch__mutmut_19': xǁTabixFileǁfetch__mutmut_19, 
        'xǁTabixFileǁfetch__mutmut_20': xǁTabixFileǁfetch__mutmut_20, 
        'xǁTabixFileǁfetch__mutmut_21': xǁTabixFileǁfetch__mutmut_21, 
        'xǁTabixFileǁfetch__mutmut_22': xǁTabixFileǁfetch__mutmut_22, 
        'xǁTabixFileǁfetch__mutmut_23': xǁTabixFileǁfetch__mutmut_23, 
        'xǁTabixFileǁfetch__mutmut_24': xǁTabixFileǁfetch__mutmut_24, 
        'xǁTabixFileǁfetch__mutmut_25': xǁTabixFileǁfetch__mutmut_25, 
        'xǁTabixFileǁfetch__mutmut_26': xǁTabixFileǁfetch__mutmut_26, 
        'xǁTabixFileǁfetch__mutmut_27': xǁTabixFileǁfetch__mutmut_27, 
        'xǁTabixFileǁfetch__mutmut_28': xǁTabixFileǁfetch__mutmut_28, 
        'xǁTabixFileǁfetch__mutmut_29': xǁTabixFileǁfetch__mutmut_29, 
        'xǁTabixFileǁfetch__mutmut_30': xǁTabixFileǁfetch__mutmut_30, 
        'xǁTabixFileǁfetch__mutmut_31': xǁTabixFileǁfetch__mutmut_31, 
        'xǁTabixFileǁfetch__mutmut_32': xǁTabixFileǁfetch__mutmut_32, 
        'xǁTabixFileǁfetch__mutmut_33': xǁTabixFileǁfetch__mutmut_33, 
        'xǁTabixFileǁfetch__mutmut_34': xǁTabixFileǁfetch__mutmut_34, 
        'xǁTabixFileǁfetch__mutmut_35': xǁTabixFileǁfetch__mutmut_35, 
        'xǁTabixFileǁfetch__mutmut_36': xǁTabixFileǁfetch__mutmut_36, 
        'xǁTabixFileǁfetch__mutmut_37': xǁTabixFileǁfetch__mutmut_37, 
        'xǁTabixFileǁfetch__mutmut_38': xǁTabixFileǁfetch__mutmut_38, 
        'xǁTabixFileǁfetch__mutmut_39': xǁTabixFileǁfetch__mutmut_39, 
        'xǁTabixFileǁfetch__mutmut_40': xǁTabixFileǁfetch__mutmut_40, 
        'xǁTabixFileǁfetch__mutmut_41': xǁTabixFileǁfetch__mutmut_41, 
        'xǁTabixFileǁfetch__mutmut_42': xǁTabixFileǁfetch__mutmut_42, 
        'xǁTabixFileǁfetch__mutmut_43': xǁTabixFileǁfetch__mutmut_43
    }

    def fetch(self, *args, **kwargs):
        return _mutmut_trampoline(object.__getattribute__(self, "xǁTabixFileǁfetch__mutmut_orig"), object.__getattribute__(self, "xǁTabixFileǁfetch__mutmut_mutants"), *args, **kwargs) 

    fetch.__signature__ = _mutmut_signature(xǁTabixFileǁfetch__mutmut_orig)
    xǁTabixFileǁfetch__mutmut_orig.__name__ = 'xǁTabixFileǁfetch'



    def xǁTabixFileǁclose__mutmut_orig(self) -> None:
        """Close the TabixFile."""
        if self.closed:  # pragma: no cover
            raise ValueError("TabixFile already closed - double close?")
        self.file.close()
        self.closed = True

    def xǁTabixFileǁclose__mutmut_1(self) -> None:
        """Close the TabixFile."""
        if self.closed:  # pragma: no cover
            raise ValueError("XXTabixFile already closed - double close?XX")
        self.file.close()
        self.closed = True

    def xǁTabixFileǁclose__mutmut_2(self) -> None:
        """Close the TabixFile."""
        if self.closed:  # pragma: no cover
            raise ValueError("TabixFile already closed - double close?")
        self.file.close()
        self.closed = False

    def xǁTabixFileǁclose__mutmut_3(self) -> None:
        """Close the TabixFile."""
        if self.closed:  # pragma: no cover
            raise ValueError("TabixFile already closed - double close?")
        self.file.close()
        self.closed = None

    xǁTabixFileǁclose__mutmut_mutants = {
    'xǁTabixFileǁclose__mutmut_1': xǁTabixFileǁclose__mutmut_1, 
        'xǁTabixFileǁclose__mutmut_2': xǁTabixFileǁclose__mutmut_2, 
        'xǁTabixFileǁclose__mutmut_3': xǁTabixFileǁclose__mutmut_3
    }

    def close(self, *args, **kwargs):
        return _mutmut_trampoline(object.__getattribute__(self, "xǁTabixFileǁclose__mutmut_orig"), object.__getattribute__(self, "xǁTabixFileǁclose__mutmut_mutants"), *args, **kwargs) 

    close.__signature__ = _mutmut_signature(xǁTabixFileǁclose__mutmut_orig)
    xǁTabixFileǁclose__mutmut_orig.__name__ = 'xǁTabixFileǁclose'



    def xǁTabixFileǁ__enter____mutmut_orig(self) -> "TabixFile":
        return self

    xǁTabixFileǁ__enter____mutmut_mutants = {

    }

    def __enter__(self, *args, **kwargs):
        return _mutmut_trampoline(object.__getattribute__(self, "xǁTabixFileǁ__enter____mutmut_orig"), object.__getattribute__(self, "xǁTabixFileǁ__enter____mutmut_mutants"), *args, **kwargs) 

    __enter__.__signature__ = _mutmut_signature(xǁTabixFileǁ__enter____mutmut_orig)
    xǁTabixFileǁ__enter____mutmut_orig.__name__ = 'xǁTabixFileǁ__enter__'



    def xǁTabixFileǁ__exit____mutmut_orig(self, exc_type, exc_val, exc_tb) -> None:
        self.close()

    xǁTabixFileǁ__exit____mutmut_mutants = {

    }

    def __exit__(self, *args, **kwargs):
        return _mutmut_trampoline(object.__getattribute__(self, "xǁTabixFileǁ__exit____mutmut_orig"), object.__getattribute__(self, "xǁTabixFileǁ__exit____mutmut_mutants"), *args, **kwargs) 

    __exit__.__signature__ = _mutmut_signature(xǁTabixFileǁ__exit____mutmut_orig)
    xǁTabixFileǁ__exit____mutmut_orig.__name__ = 'xǁTabixFileǁ__exit__'




class TabixFileIter:
    """Allows for easy iteration over a tabix file."""

    def xǁTabixFileIterǁ__init____mutmut_orig(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = start
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 0
        self._current_lines: list[str] = []
        self._current_line_idx = 0
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_1(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = None
        self.reference = reference
        self.start = start
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 0
        self._current_lines: list[str] = []
        self._current_line_idx = 0
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_2(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = None
        self.start = start
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 0
        self._current_lines: list[str] = []
        self._current_line_idx = 0
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_3(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = None
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 0
        self._current_lines: list[str] = []
        self._current_line_idx = 0
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_4(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = start
        self.end = None
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 0
        self._current_lines: list[str] = []
        self._current_line_idx = 0
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_5(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = start
        self.end = end
        self.bgzf_file = None

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 0
        self._current_lines: list[str] = []
        self._current_line_idx = 0
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_6(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = start
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference  in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 0
        self._current_lines: list[str] = []
        self._current_line_idx = 0
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_7(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = start
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[None]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 0
        self._current_lines: list[str] = []
        self._current_line_idx = 0
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_8(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = start
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = None

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 0
        self._current_lines: list[str] = []
        self._current_line_idx = 0
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_9(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = start
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = None
        self._current_chunk_idx = 0
        self._current_lines: list[str] = []
        self._current_line_idx = 0
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_10(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = start
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 1
        self._current_lines: list[str] = []
        self._current_line_idx = 0
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_11(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = start
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = None
        self._current_lines: list[str] = []
        self._current_line_idx = 0
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_12(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = start
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 0
        self._current_lines: list[str] = None
        self._current_line_idx = 0
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_13(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = start
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 0
        self._current_lines: list[str] = []
        self._current_line_idx = 1
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_14(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = start
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 0
        self._current_lines: list[str] = []
        self._current_line_idx = None
        self._finished = False

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_15(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = start
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 0
        self._current_lines: list[str] = []
        self._current_line_idx = 0
        self._finished = True

        # Find relevant chunks for the region
        self._find_chunks()

    def xǁTabixFileIterǁ__init____mutmut_16(
        self,
        *,
        index: TabixIndex,
        reference: str,
        start: int | None = None,
        end: int | None = None,
        bgzf_file: BgzfReader | None = None,
    ):
        """Initialize TabixFileIter.

        :param index: The tabix index to use for lookups
        :param reference: Reference sequence name
        :param start: Start position (1-based, inclusive) or None for start of sequence
        :param end: End position (1-based, inclusive) or None for end of sequence
        :param bgzf_file: BGZF file reader (if None, iteration will not work)
        """
        self.index = index
        self.reference = reference
        self.start = start
        self.end = end
        self.bgzf_file = bgzf_file

        # Validate that the reference exists in the index
        if reference not in index.indices:  # pragma: no cover
            raise ValueError(f"Reference '{reference}' not found in tabix index")

        # Store the sequence index for this reference
        self.sequence_index = index.indices[reference]

        # Initialize iteration state
        self._chunks: list[Chunk] = []
        self._current_chunk_idx = 0
        self._current_lines: list[str] = []
        self._current_line_idx = 0
        self._finished = None

        # Find relevant chunks for the region
        self._find_chunks()

    xǁTabixFileIterǁ__init____mutmut_mutants = {
    'xǁTabixFileIterǁ__init____mutmut_1': xǁTabixFileIterǁ__init____mutmut_1, 
        'xǁTabixFileIterǁ__init____mutmut_2': xǁTabixFileIterǁ__init____mutmut_2, 
        'xǁTabixFileIterǁ__init____mutmut_3': xǁTabixFileIterǁ__init____mutmut_3, 
        'xǁTabixFileIterǁ__init____mutmut_4': xǁTabixFileIterǁ__init____mutmut_4, 
        'xǁTabixFileIterǁ__init____mutmut_5': xǁTabixFileIterǁ__init____mutmut_5, 
        'xǁTabixFileIterǁ__init____mutmut_6': xǁTabixFileIterǁ__init____mutmut_6, 
        'xǁTabixFileIterǁ__init____mutmut_7': xǁTabixFileIterǁ__init____mutmut_7, 
        'xǁTabixFileIterǁ__init____mutmut_8': xǁTabixFileIterǁ__init____mutmut_8, 
        'xǁTabixFileIterǁ__init____mutmut_9': xǁTabixFileIterǁ__init____mutmut_9, 
        'xǁTabixFileIterǁ__init____mutmut_10': xǁTabixFileIterǁ__init____mutmut_10, 
        'xǁTabixFileIterǁ__init____mutmut_11': xǁTabixFileIterǁ__init____mutmut_11, 
        'xǁTabixFileIterǁ__init____mutmut_12': xǁTabixFileIterǁ__init____mutmut_12, 
        'xǁTabixFileIterǁ__init____mutmut_13': xǁTabixFileIterǁ__init____mutmut_13, 
        'xǁTabixFileIterǁ__init____mutmut_14': xǁTabixFileIterǁ__init____mutmut_14, 
        'xǁTabixFileIterǁ__init____mutmut_15': xǁTabixFileIterǁ__init____mutmut_15, 
        'xǁTabixFileIterǁ__init____mutmut_16': xǁTabixFileIterǁ__init____mutmut_16
    }

    def __init__(self, *args, **kwargs):
        return _mutmut_trampoline(object.__getattribute__(self, "xǁTabixFileIterǁ__init____mutmut_orig"), object.__getattribute__(self, "xǁTabixFileIterǁ__init____mutmut_mutants"), *args, **kwargs) 

    __init__.__signature__ = _mutmut_signature(xǁTabixFileIterǁ__init____mutmut_orig)
    xǁTabixFileIterǁ__init____mutmut_orig.__name__ = 'xǁTabixFileIterǁ__init__'



    def xǁTabixFileIterǁ_find_chunks__mutmut_orig(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_1(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is not None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_2(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is not None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_3(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None or self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_4(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = None

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_5(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = None

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_6(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: None, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_7(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=False)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_8(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_9(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number,)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_10(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = None

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_11(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = None  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_12(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end != 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_13(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 1:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_14(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = True
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_15(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = None
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_16(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if  (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_17(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end < covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_18(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg > covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_19(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start and chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_20(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = False
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_21(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = None
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_22(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if  overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_23(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(None)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_24(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(None, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_25(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: None)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_26(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted( key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_27(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks,)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_28(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = None
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_29(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start + 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_30(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 2) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_31(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is  None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_32(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 1
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_33(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = None
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_34(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is  None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_35(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 3**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_36(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2*29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_37(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**30  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_38(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = None  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_39(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(None, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_40(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, None)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_41(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins( end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_42(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based,)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_43(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = None

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_44(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = None

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_45(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = None
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_46(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number not in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_47(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[None]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_48(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = None
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_49(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end == 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_50(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 1:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_51(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(None)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_52(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(None)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_53(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = None

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_54(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is  None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_55(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end > linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_56(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = None

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_57(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: None)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_58(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[1]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_59(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[None]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_60(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = None
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_61(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[2:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_62(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[None]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_63(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[+1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_64(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-2]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_65(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[None]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_66(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = None
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_67(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg < last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_68(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[+1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_69(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-2] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_70(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[None] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_71(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk( end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_72(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg),
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_73(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = None
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_74(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(None)

                self._chunks = merged_chunks
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_75(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = None
            else:
                self._chunks = []

    def xǁTabixFileIterǁ_find_chunks__mutmut_76(self) -> None:
        """Find chunks that may contain data for the requested region."""
        if self.start is None and self.end is None:
            # No region specified - get all chunks for this reference, but filter out special chunks
            # Use the lowest-level bins (bin 0 and highest numbered bins) to avoid duplicates
            chunks = []

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # For whole chromosome, prefer the most specific bins to avoid reading overlapping data
            # Sort bins by number and take chunks from highest numbered bins first
            sorted_bins = sorted(self.sequence_index.bins, key=lambda b: b.number, reverse=True)

            covered_ranges = []  # Track (start, end) ranges we've already covered

            for bin_obj in sorted_bins:
                for chunk in bin_obj.chunks:
                    # Skip special chunks with end=0
                    if chunk.end == 0:
                        continue

                    # Check if this chunk overlaps with already covered ranges
                    overlaps = False
                    for covered_start, covered_end in covered_ranges:
                        if not (chunk.end <= covered_start or chunk.beg >= covered_end):
                            overlaps = True
                            break

                    if not overlaps:
                        chunks.append(chunk)
                        covered_ranges.append((chunk.beg, chunk.end))

            self._chunks = sorted(chunks, key=lambda c: c.beg)
        else:
            # Region specified - convert 1-based coordinates to 0-based for internal use
            start_0based = (self.start - 1) if self.start is not None else 0
            end_0based = self.end if self.end is not None else 2**29  # Large value for unbounded end

            # Find overlapping bins using UCSC binning scheme
            bin_numbers = reg2bins(start_0based, end_0based)

            # Create a mapping of bin numbers to bins for efficient lookup
            bin_map = {bin_obj.number: bin_obj for bin_obj in self.sequence_index.bins}

            # Collect chunks from overlapping bins in the order returned by reg2bins (reverse order)
            chunks = []
            for bin_number in bin_numbers:
                if bin_number in bin_map:
                    bin_obj = bin_map[bin_number]
                    for chunk in bin_obj.chunks:
                        # Skip special chunks with end=0
                        if chunk.end != 0:
                            chunks.append(chunk)

            # Use linear index to find minimum virtual offset
            linear_min_offset = self._lookup_linear_offset(start_0based)

            # Filter chunks that end before our linear minimum
            if linear_min_offset is not None:
                chunks = [c for c in chunks if c.end >= linear_min_offset]

            # Merge overlapping chunks to create a single virtual range
            if chunks:
                # Sort chunks by start position
                chunks.sort(key=lambda c: c.beg)

                # Merge overlapping chunks
                merged_chunks = [chunks[0]]
                for chunk in chunks[1:]:
                    last_chunk = merged_chunks[-1]
                    if chunk.beg <= last_chunk.end:
                        # Merge overlapping chunks
                        merged_chunks[-1] = Chunk(
                            beg=min(last_chunk.beg, chunk.beg), end=max(last_chunk.end, chunk.end)
                        )
                    else:  # pragma: no cover
                        merged_chunks.append(chunk)

                self._chunks = merged_chunks
            else:
                self._chunks = None

    xǁTabixFileIterǁ_find_chunks__mutmut_mutants = {
    'xǁTabixFileIterǁ_find_chunks__mutmut_1': xǁTabixFileIterǁ_find_chunks__mutmut_1, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_2': xǁTabixFileIterǁ_find_chunks__mutmut_2, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_3': xǁTabixFileIterǁ_find_chunks__mutmut_3, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_4': xǁTabixFileIterǁ_find_chunks__mutmut_4, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_5': xǁTabixFileIterǁ_find_chunks__mutmut_5, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_6': xǁTabixFileIterǁ_find_chunks__mutmut_6, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_7': xǁTabixFileIterǁ_find_chunks__mutmut_7, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_8': xǁTabixFileIterǁ_find_chunks__mutmut_8, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_9': xǁTabixFileIterǁ_find_chunks__mutmut_9, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_10': xǁTabixFileIterǁ_find_chunks__mutmut_10, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_11': xǁTabixFileIterǁ_find_chunks__mutmut_11, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_12': xǁTabixFileIterǁ_find_chunks__mutmut_12, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_13': xǁTabixFileIterǁ_find_chunks__mutmut_13, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_14': xǁTabixFileIterǁ_find_chunks__mutmut_14, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_15': xǁTabixFileIterǁ_find_chunks__mutmut_15, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_16': xǁTabixFileIterǁ_find_chunks__mutmut_16, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_17': xǁTabixFileIterǁ_find_chunks__mutmut_17, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_18': xǁTabixFileIterǁ_find_chunks__mutmut_18, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_19': xǁTabixFileIterǁ_find_chunks__mutmut_19, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_20': xǁTabixFileIterǁ_find_chunks__mutmut_20, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_21': xǁTabixFileIterǁ_find_chunks__mutmut_21, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_22': xǁTabixFileIterǁ_find_chunks__mutmut_22, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_23': xǁTabixFileIterǁ_find_chunks__mutmut_23, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_24': xǁTabixFileIterǁ_find_chunks__mutmut_24, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_25': xǁTabixFileIterǁ_find_chunks__mutmut_25, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_26': xǁTabixFileIterǁ_find_chunks__mutmut_26, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_27': xǁTabixFileIterǁ_find_chunks__mutmut_27, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_28': xǁTabixFileIterǁ_find_chunks__mutmut_28, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_29': xǁTabixFileIterǁ_find_chunks__mutmut_29, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_30': xǁTabixFileIterǁ_find_chunks__mutmut_30, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_31': xǁTabixFileIterǁ_find_chunks__mutmut_31, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_32': xǁTabixFileIterǁ_find_chunks__mutmut_32, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_33': xǁTabixFileIterǁ_find_chunks__mutmut_33, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_34': xǁTabixFileIterǁ_find_chunks__mutmut_34, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_35': xǁTabixFileIterǁ_find_chunks__mutmut_35, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_36': xǁTabixFileIterǁ_find_chunks__mutmut_36, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_37': xǁTabixFileIterǁ_find_chunks__mutmut_37, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_38': xǁTabixFileIterǁ_find_chunks__mutmut_38, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_39': xǁTabixFileIterǁ_find_chunks__mutmut_39, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_40': xǁTabixFileIterǁ_find_chunks__mutmut_40, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_41': xǁTabixFileIterǁ_find_chunks__mutmut_41, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_42': xǁTabixFileIterǁ_find_chunks__mutmut_42, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_43': xǁTabixFileIterǁ_find_chunks__mutmut_43, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_44': xǁTabixFileIterǁ_find_chunks__mutmut_44, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_45': xǁTabixFileIterǁ_find_chunks__mutmut_45, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_46': xǁTabixFileIterǁ_find_chunks__mutmut_46, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_47': xǁTabixFileIterǁ_find_chunks__mutmut_47, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_48': xǁTabixFileIterǁ_find_chunks__mutmut_48, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_49': xǁTabixFileIterǁ_find_chunks__mutmut_49, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_50': xǁTabixFileIterǁ_find_chunks__mutmut_50, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_51': xǁTabixFileIterǁ_find_chunks__mutmut_51, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_52': xǁTabixFileIterǁ_find_chunks__mutmut_52, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_53': xǁTabixFileIterǁ_find_chunks__mutmut_53, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_54': xǁTabixFileIterǁ_find_chunks__mutmut_54, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_55': xǁTabixFileIterǁ_find_chunks__mutmut_55, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_56': xǁTabixFileIterǁ_find_chunks__mutmut_56, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_57': xǁTabixFileIterǁ_find_chunks__mutmut_57, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_58': xǁTabixFileIterǁ_find_chunks__mutmut_58, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_59': xǁTabixFileIterǁ_find_chunks__mutmut_59, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_60': xǁTabixFileIterǁ_find_chunks__mutmut_60, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_61': xǁTabixFileIterǁ_find_chunks__mutmut_61, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_62': xǁTabixFileIterǁ_find_chunks__mutmut_62, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_63': xǁTabixFileIterǁ_find_chunks__mutmut_63, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_64': xǁTabixFileIterǁ_find_chunks__mutmut_64, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_65': xǁTabixFileIterǁ_find_chunks__mutmut_65, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_66': xǁTabixFileIterǁ_find_chunks__mutmut_66, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_67': xǁTabixFileIterǁ_find_chunks__mutmut_67, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_68': xǁTabixFileIterǁ_find_chunks__mutmut_68, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_69': xǁTabixFileIterǁ_find_chunks__mutmut_69, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_70': xǁTabixFileIterǁ_find_chunks__mutmut_70, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_71': xǁTabixFileIterǁ_find_chunks__mutmut_71, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_72': xǁTabixFileIterǁ_find_chunks__mutmut_72, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_73': xǁTabixFileIterǁ_find_chunks__mutmut_73, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_74': xǁTabixFileIterǁ_find_chunks__mutmut_74, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_75': xǁTabixFileIterǁ_find_chunks__mutmut_75, 
        'xǁTabixFileIterǁ_find_chunks__mutmut_76': xǁTabixFileIterǁ_find_chunks__mutmut_76
    }

    def _find_chunks(self, *args, **kwargs):
        return _mutmut_trampoline(object.__getattribute__(self, "xǁTabixFileIterǁ_find_chunks__mutmut_orig"), object.__getattribute__(self, "xǁTabixFileIterǁ_find_chunks__mutmut_mutants"), *args, **kwargs) 

    _find_chunks.__signature__ = _mutmut_signature(xǁTabixFileIterǁ_find_chunks__mutmut_orig)
    xǁTabixFileIterǁ_find_chunks__mutmut_orig.__name__ = 'xǁTabixFileIterǁ_find_chunks'



    def xǁTabixFileIterǁ_lookup_linear_offset__mutmut_orig(self, pos: int) -> int | None:
        """Use linear index to find minimum virtual offset for position."""
        # Linear index uses 16kb intervals
        interval_idx = pos >> 14  # pos // 16384

        if interval_idx < len(self.sequence_index.offsets):
            return self.sequence_index.offsets[interval_idx]
        return None

    def xǁTabixFileIterǁ_lookup_linear_offset__mutmut_1(self, pos: int) -> int | None:
        """Use linear index to find minimum virtual offset for position."""
        # Linear index uses 16kb intervals
        interval_idx = pos << 14  # pos // 16384

        if interval_idx < len(self.sequence_index.offsets):
            return self.sequence_index.offsets[interval_idx]
        return None

    def xǁTabixFileIterǁ_lookup_linear_offset__mutmut_2(self, pos: int) -> int | None:
        """Use linear index to find minimum virtual offset for position."""
        # Linear index uses 16kb intervals
        interval_idx = pos >> 15  # pos // 16384

        if interval_idx < len(self.sequence_index.offsets):
            return self.sequence_index.offsets[interval_idx]
        return None

    def xǁTabixFileIterǁ_lookup_linear_offset__mutmut_3(self, pos: int) -> int | None:
        """Use linear index to find minimum virtual offset for position."""
        # Linear index uses 16kb intervals
        interval_idx = None  # pos // 16384

        if interval_idx < len(self.sequence_index.offsets):
            return self.sequence_index.offsets[interval_idx]
        return None

    def xǁTabixFileIterǁ_lookup_linear_offset__mutmut_4(self, pos: int) -> int | None:
        """Use linear index to find minimum virtual offset for position."""
        # Linear index uses 16kb intervals
        interval_idx = pos >> 14  # pos // 16384

        if interval_idx <= len(self.sequence_index.offsets):
            return self.sequence_index.offsets[interval_idx]
        return None

    def xǁTabixFileIterǁ_lookup_linear_offset__mutmut_5(self, pos: int) -> int | None:
        """Use linear index to find minimum virtual offset for position."""
        # Linear index uses 16kb intervals
        interval_idx = pos >> 14  # pos // 16384

        if interval_idx < len(self.sequence_index.offsets):
            return self.sequence_index.offsets[None]
        return None

    xǁTabixFileIterǁ_lookup_linear_offset__mutmut_mutants = {
    'xǁTabixFileIterǁ_lookup_linear_offset__mutmut_1': xǁTabixFileIterǁ_lookup_linear_offset__mutmut_1, 
        'xǁTabixFileIterǁ_lookup_linear_offset__mutmut_2': xǁTabixFileIterǁ_lookup_linear_offset__mutmut_2, 
        'xǁTabixFileIterǁ_lookup_linear_offset__mutmut_3': xǁTabixFileIterǁ_lookup_linear_offset__mutmut_3, 
        'xǁTabixFileIterǁ_lookup_linear_offset__mutmut_4': xǁTabixFileIterǁ_lookup_linear_offset__mutmut_4, 
        'xǁTabixFileIterǁ_lookup_linear_offset__mutmut_5': xǁTabixFileIterǁ_lookup_linear_offset__mutmut_5
    }

    def _lookup_linear_offset(self, *args, **kwargs):
        return _mutmut_trampoline(object.__getattribute__(self, "xǁTabixFileIterǁ_lookup_linear_offset__mutmut_orig"), object.__getattribute__(self, "xǁTabixFileIterǁ_lookup_linear_offset__mutmut_mutants"), *args, **kwargs) 

    _lookup_linear_offset.__signature__ = _mutmut_signature(xǁTabixFileIterǁ_lookup_linear_offset__mutmut_orig)
    xǁTabixFileIterǁ_lookup_linear_offset__mutmut_orig.__name__ = 'xǁTabixFileIterǁ_lookup_linear_offset'



    def xǁTabixFileIterǁ_load_next_chunk__mutmut_orig(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_1(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx > len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_2(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is not None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_3(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) and self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_4(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return True

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_5(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[None]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_6(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = None
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_7(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx -= 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_8(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx = 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_9(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 2

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_10(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = None
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_11(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = None

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_12(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset <= chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_13(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = None
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_14(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if  line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_15(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("XX\n\rXX"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_16(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = None

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_17(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = None
        self._current_line_idx = 0
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_18(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 1
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_19(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = None
        return len(lines) > 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_20(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) >= 0

    def xǁTabixFileIterǁ_load_next_chunk__mutmut_21(self) -> bool:
        """Load lines from the next chunk. Returns True if successful."""
        if self._current_chunk_idx >= len(self._chunks) or self.bgzf_file is None:
            return False

        chunk = self._chunks[self._current_chunk_idx]
        self._current_chunk_idx += 1

        # Seek to chunk start and read until chunk end
        self.bgzf_file.seek(chunk.beg)

        lines = []
        current_offset = chunk.beg

        while current_offset < chunk.end:
            line = self.bgzf_file.readline()
            if not line:  # pragma: no cover
                break

            lines.append(line.rstrip("\n\r"))
            current_offset = self.bgzf_file.tell()

        self._current_lines = lines
        self._current_line_idx = 0
        return len(lines) > 1

    xǁTabixFileIterǁ_load_next_chunk__mutmut_mutants = {
    'xǁTabixFileIterǁ_load_next_chunk__mutmut_1': xǁTabixFileIterǁ_load_next_chunk__mutmut_1, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_2': xǁTabixFileIterǁ_load_next_chunk__mutmut_2, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_3': xǁTabixFileIterǁ_load_next_chunk__mutmut_3, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_4': xǁTabixFileIterǁ_load_next_chunk__mutmut_4, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_5': xǁTabixFileIterǁ_load_next_chunk__mutmut_5, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_6': xǁTabixFileIterǁ_load_next_chunk__mutmut_6, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_7': xǁTabixFileIterǁ_load_next_chunk__mutmut_7, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_8': xǁTabixFileIterǁ_load_next_chunk__mutmut_8, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_9': xǁTabixFileIterǁ_load_next_chunk__mutmut_9, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_10': xǁTabixFileIterǁ_load_next_chunk__mutmut_10, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_11': xǁTabixFileIterǁ_load_next_chunk__mutmut_11, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_12': xǁTabixFileIterǁ_load_next_chunk__mutmut_12, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_13': xǁTabixFileIterǁ_load_next_chunk__mutmut_13, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_14': xǁTabixFileIterǁ_load_next_chunk__mutmut_14, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_15': xǁTabixFileIterǁ_load_next_chunk__mutmut_15, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_16': xǁTabixFileIterǁ_load_next_chunk__mutmut_16, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_17': xǁTabixFileIterǁ_load_next_chunk__mutmut_17, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_18': xǁTabixFileIterǁ_load_next_chunk__mutmut_18, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_19': xǁTabixFileIterǁ_load_next_chunk__mutmut_19, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_20': xǁTabixFileIterǁ_load_next_chunk__mutmut_20, 
        'xǁTabixFileIterǁ_load_next_chunk__mutmut_21': xǁTabixFileIterǁ_load_next_chunk__mutmut_21
    }

    def _load_next_chunk(self, *args, **kwargs):
        return _mutmut_trampoline(object.__getattribute__(self, "xǁTabixFileIterǁ_load_next_chunk__mutmut_orig"), object.__getattribute__(self, "xǁTabixFileIterǁ_load_next_chunk__mutmut_mutants"), *args, **kwargs) 

    _load_next_chunk.__signature__ = _mutmut_signature(xǁTabixFileIterǁ_load_next_chunk__mutmut_orig)
    xǁTabixFileIterǁ_load_next_chunk__mutmut_orig.__name__ = 'xǁTabixFileIterǁ_load_next_chunk'



    def xǁTabixFileIterǁ_parse_line_position__mutmut_orig(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_1(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("XX\tXX")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_2(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = None

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_3(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq + 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_4(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 2] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_5(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[None] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_6(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq >= 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_7(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 1 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_8(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else "XXXX"

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_9(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = None

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_10(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg + 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_11(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 2]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_12(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[None]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_13(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg >= 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_14(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 1 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_15(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 2

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_16(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = None

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_17(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end != 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_18(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 1 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_19(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end != self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_20(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 and self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_21(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) >= 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_22(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 4:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_23(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[4]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_24(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[None]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_25(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = None  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_26(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos - len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_27(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) + 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_28(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 2
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_29(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = None
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_30(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = None
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_31(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end + 1])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_32(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[self.index.col_end - 2])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_33(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = int(fields[None])

        return seq_name, begin_pos, end_pos

    def xǁTabixFileIterǁ_parse_line_position__mutmut_34(self, line: str) -> tuple[str, int, int]:
        """Parse sequence name and position from a line.

        :return: (sequence_name, begin_pos, end_pos) using 1-based coordinates
        """
        fields = line.split("\t")

        # Extract sequence name (convert from 1-based column to 0-based index)
        seq_name = fields[self.index.col_seq - 1] if self.index.col_seq > 0 else ""

        # Extract begin position (1-based coordinate)
        begin_pos = int(fields[self.index.col_beg - 1]) if self.index.col_beg > 0 else 1

        # Extract end position (1-based coordinate)
        if self.index.col_end == 0 or self.index.col_end == self.index.col_beg:
            # For VCF files: col_end = 0 means calculate end from REF allele length
            # The inclusive end position is start + len(REF) - 1
            if len(fields) > 3:  # Make sure we have the REF column
                ref_allele = fields[3]  # REF is the 4th column (index 3)
                end_pos = begin_pos + len(ref_allele) - 1
            else:  # pragma: no cover
                # Fallback for malformed lines
                end_pos = begin_pos
        else:  # pragma: no cover
            end_pos = None

        return seq_name, begin_pos, end_pos

    xǁTabixFileIterǁ_parse_line_position__mutmut_mutants = {
    'xǁTabixFileIterǁ_parse_line_position__mutmut_1': xǁTabixFileIterǁ_parse_line_position__mutmut_1, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_2': xǁTabixFileIterǁ_parse_line_position__mutmut_2, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_3': xǁTabixFileIterǁ_parse_line_position__mutmut_3, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_4': xǁTabixFileIterǁ_parse_line_position__mutmut_4, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_5': xǁTabixFileIterǁ_parse_line_position__mutmut_5, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_6': xǁTabixFileIterǁ_parse_line_position__mutmut_6, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_7': xǁTabixFileIterǁ_parse_line_position__mutmut_7, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_8': xǁTabixFileIterǁ_parse_line_position__mutmut_8, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_9': xǁTabixFileIterǁ_parse_line_position__mutmut_9, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_10': xǁTabixFileIterǁ_parse_line_position__mutmut_10, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_11': xǁTabixFileIterǁ_parse_line_position__mutmut_11, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_12': xǁTabixFileIterǁ_parse_line_position__mutmut_12, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_13': xǁTabixFileIterǁ_parse_line_position__mutmut_13, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_14': xǁTabixFileIterǁ_parse_line_position__mutmut_14, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_15': xǁTabixFileIterǁ_parse_line_position__mutmut_15, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_16': xǁTabixFileIterǁ_parse_line_position__mutmut_16, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_17': xǁTabixFileIterǁ_parse_line_position__mutmut_17, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_18': xǁTabixFileIterǁ_parse_line_position__mutmut_18, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_19': xǁTabixFileIterǁ_parse_line_position__mutmut_19, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_20': xǁTabixFileIterǁ_parse_line_position__mutmut_20, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_21': xǁTabixFileIterǁ_parse_line_position__mutmut_21, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_22': xǁTabixFileIterǁ_parse_line_position__mutmut_22, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_23': xǁTabixFileIterǁ_parse_line_position__mutmut_23, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_24': xǁTabixFileIterǁ_parse_line_position__mutmut_24, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_25': xǁTabixFileIterǁ_parse_line_position__mutmut_25, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_26': xǁTabixFileIterǁ_parse_line_position__mutmut_26, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_27': xǁTabixFileIterǁ_parse_line_position__mutmut_27, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_28': xǁTabixFileIterǁ_parse_line_position__mutmut_28, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_29': xǁTabixFileIterǁ_parse_line_position__mutmut_29, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_30': xǁTabixFileIterǁ_parse_line_position__mutmut_30, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_31': xǁTabixFileIterǁ_parse_line_position__mutmut_31, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_32': xǁTabixFileIterǁ_parse_line_position__mutmut_32, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_33': xǁTabixFileIterǁ_parse_line_position__mutmut_33, 
        'xǁTabixFileIterǁ_parse_line_position__mutmut_34': xǁTabixFileIterǁ_parse_line_position__mutmut_34
    }

    def _parse_line_position(self, *args, **kwargs):
        return _mutmut_trampoline(object.__getattribute__(self, "xǁTabixFileIterǁ_parse_line_position__mutmut_orig"), object.__getattribute__(self, "xǁTabixFileIterǁ_parse_line_position__mutmut_mutants"), *args, **kwargs) 

    _parse_line_position.__signature__ = _mutmut_signature(xǁTabixFileIterǁ_parse_line_position__mutmut_orig)
    xǁTabixFileIterǁ_parse_line_position__mutmut_orig.__name__ = 'xǁTabixFileIterǁ_parse_line_position'



    def xǁTabixFileIterǁ_line_matches_region__mutmut_orig(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = self._parse_line_position(line)

            # Check sequence name
            if seq_name != self.reference:  # pragma: no cover
                return False

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is not None and end_pos < self.start:
                return False

            if self.end is not None and begin_pos > self.end:
                return False

            return True
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return False

    def xǁTabixFileIterǁ_line_matches_region__mutmut_1(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = self._parse_line_position(None)

            # Check sequence name
            if seq_name != self.reference:  # pragma: no cover
                return False

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is not None and end_pos < self.start:
                return False

            if self.end is not None and begin_pos > self.end:
                return False

            return True
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return False

    def xǁTabixFileIterǁ_line_matches_region__mutmut_2(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = None

            # Check sequence name
            if seq_name != self.reference:  # pragma: no cover
                return False

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is not None and end_pos < self.start:
                return False

            if self.end is not None and begin_pos > self.end:
                return False

            return True
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return False

    def xǁTabixFileIterǁ_line_matches_region__mutmut_3(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = self._parse_line_position(line)

            # Check sequence name
            if seq_name == self.reference:  # pragma: no cover
                return False

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is not None and end_pos < self.start:
                return False

            if self.end is not None and begin_pos > self.end:
                return False

            return True
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return False

    def xǁTabixFileIterǁ_line_matches_region__mutmut_4(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = self._parse_line_position(line)

            # Check sequence name
            if seq_name != self.reference:  # pragma: no cover
                return True

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is not None and end_pos < self.start:
                return False

            if self.end is not None and begin_pos > self.end:
                return False

            return True
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return False

    def xǁTabixFileIterǁ_line_matches_region__mutmut_5(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = self._parse_line_position(line)

            # Check sequence name
            if seq_name != self.reference:  # pragma: no cover
                return False

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is  None and end_pos < self.start:
                return False

            if self.end is not None and begin_pos > self.end:
                return False

            return True
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return False

    def xǁTabixFileIterǁ_line_matches_region__mutmut_6(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = self._parse_line_position(line)

            # Check sequence name
            if seq_name != self.reference:  # pragma: no cover
                return False

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is not None and end_pos <= self.start:
                return False

            if self.end is not None and begin_pos > self.end:
                return False

            return True
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return False

    def xǁTabixFileIterǁ_line_matches_region__mutmut_7(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = self._parse_line_position(line)

            # Check sequence name
            if seq_name != self.reference:  # pragma: no cover
                return False

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is not None or end_pos < self.start:
                return False

            if self.end is not None and begin_pos > self.end:
                return False

            return True
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return False

    def xǁTabixFileIterǁ_line_matches_region__mutmut_8(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = self._parse_line_position(line)

            # Check sequence name
            if seq_name != self.reference:  # pragma: no cover
                return False

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is not None and end_pos < self.start:
                return True

            if self.end is not None and begin_pos > self.end:
                return False

            return True
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return False

    def xǁTabixFileIterǁ_line_matches_region__mutmut_9(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = self._parse_line_position(line)

            # Check sequence name
            if seq_name != self.reference:  # pragma: no cover
                return False

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is not None and end_pos < self.start:
                return False

            if self.end is  None and begin_pos > self.end:
                return False

            return True
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return False

    def xǁTabixFileIterǁ_line_matches_region__mutmut_10(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = self._parse_line_position(line)

            # Check sequence name
            if seq_name != self.reference:  # pragma: no cover
                return False

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is not None and end_pos < self.start:
                return False

            if self.end is not None and begin_pos >= self.end:
                return False

            return True
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return False

    def xǁTabixFileIterǁ_line_matches_region__mutmut_11(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = self._parse_line_position(line)

            # Check sequence name
            if seq_name != self.reference:  # pragma: no cover
                return False

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is not None and end_pos < self.start:
                return False

            if self.end is not None or begin_pos > self.end:
                return False

            return True
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return False

    def xǁTabixFileIterǁ_line_matches_region__mutmut_12(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = self._parse_line_position(line)

            # Check sequence name
            if seq_name != self.reference:  # pragma: no cover
                return False

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is not None and end_pos < self.start:
                return False

            if self.end is not None and begin_pos > self.end:
                return True

            return True
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return False

    def xǁTabixFileIterǁ_line_matches_region__mutmut_13(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = self._parse_line_position(line)

            # Check sequence name
            if seq_name != self.reference:  # pragma: no cover
                return False

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is not None and end_pos < self.start:
                return False

            if self.end is not None and begin_pos > self.end:
                return False

            return False
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return False

    def xǁTabixFileIterǁ_line_matches_region__mutmut_14(self, line: str) -> bool:
        """Check if a line matches our region criteria."""
        try:
            seq_name, begin_pos, end_pos = self._parse_line_position(line)

            # Check sequence name
            if seq_name != self.reference:  # pragma: no cover
                return False

            # Check position overlap with our region
            # For VCF: treat query ranges as inclusive on both ends
            # A record at position N should be found by queries that include position N or N+1
            # This matches pysam's behavior for VCF files
            if self.start is not None and end_pos < self.start:
                return False

            if self.end is not None and begin_pos > self.end:
                return False

            return True
        except (ValueError, IndexError):  # pragma: no cover
            # Skip malformed lines
            return True

    xǁTabixFileIterǁ_line_matches_region__mutmut_mutants = {
    'xǁTabixFileIterǁ_line_matches_region__mutmut_1': xǁTabixFileIterǁ_line_matches_region__mutmut_1, 
        'xǁTabixFileIterǁ_line_matches_region__mutmut_2': xǁTabixFileIterǁ_line_matches_region__mutmut_2, 
        'xǁTabixFileIterǁ_line_matches_region__mutmut_3': xǁTabixFileIterǁ_line_matches_region__mutmut_3, 
        'xǁTabixFileIterǁ_line_matches_region__mutmut_4': xǁTabixFileIterǁ_line_matches_region__mutmut_4, 
        'xǁTabixFileIterǁ_line_matches_region__mutmut_5': xǁTabixFileIterǁ_line_matches_region__mutmut_5, 
        'xǁTabixFileIterǁ_line_matches_region__mutmut_6': xǁTabixFileIterǁ_line_matches_region__mutmut_6, 
        'xǁTabixFileIterǁ_line_matches_region__mutmut_7': xǁTabixFileIterǁ_line_matches_region__mutmut_7, 
        'xǁTabixFileIterǁ_line_matches_region__mutmut_8': xǁTabixFileIterǁ_line_matches_region__mutmut_8, 
        'xǁTabixFileIterǁ_line_matches_region__mutmut_9': xǁTabixFileIterǁ_line_matches_region__mutmut_9, 
        'xǁTabixFileIterǁ_line_matches_region__mutmut_10': xǁTabixFileIterǁ_line_matches_region__mutmut_10, 
        'xǁTabixFileIterǁ_line_matches_region__mutmut_11': xǁTabixFileIterǁ_line_matches_region__mutmut_11, 
        'xǁTabixFileIterǁ_line_matches_region__mutmut_12': xǁTabixFileIterǁ_line_matches_region__mutmut_12, 
        'xǁTabixFileIterǁ_line_matches_region__mutmut_13': xǁTabixFileIterǁ_line_matches_region__mutmut_13, 
        'xǁTabixFileIterǁ_line_matches_region__mutmut_14': xǁTabixFileIterǁ_line_matches_region__mutmut_14
    }

    def _line_matches_region(self, *args, **kwargs):
        return _mutmut_trampoline(object.__getattribute__(self, "xǁTabixFileIterǁ_line_matches_region__mutmut_orig"), object.__getattribute__(self, "xǁTabixFileIterǁ_line_matches_region__mutmut_mutants"), *args, **kwargs) 

    _line_matches_region.__signature__ = _mutmut_signature(xǁTabixFileIterǁ_line_matches_region__mutmut_orig)
    xǁTabixFileIterǁ_line_matches_region__mutmut_orig.__name__ = 'xǁTabixFileIterǁ_line_matches_region'



    def xǁTabixFileIterǁ__iter____mutmut_orig(self) -> "TabixFileIter":
        """Return self as iterator."""
        return self

    xǁTabixFileIterǁ__iter____mutmut_mutants = {

    }

    def __iter__(self, *args, **kwargs):
        return _mutmut_trampoline(object.__getattribute__(self, "xǁTabixFileIterǁ__iter____mutmut_orig"), object.__getattribute__(self, "xǁTabixFileIterǁ__iter____mutmut_mutants"), *args, **kwargs) 

    __iter__.__signature__ = _mutmut_signature(xǁTabixFileIterǁ__iter____mutmut_orig)
    xǁTabixFileIterǁ__iter____mutmut_orig.__name__ = 'xǁTabixFileIterǁ__iter__'



    def xǁTabixFileIterǁ__next____mutmut_orig(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_1(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while False:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_2(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx > len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_3(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if  self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_4(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = False
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_5(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = None
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_6(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx <= len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_7(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[None]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_8(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = None
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_9(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx -= 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_10(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx = 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_11(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 2

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_12(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("XX#XX"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_13(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(None):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_14(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(None)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_15(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = None

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_16(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = None
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_17(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name not in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_18(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference not in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_19(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names or self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_20(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(None)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_21(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = None
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_22(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = None
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_23(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx >= ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_24(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = False
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_25(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = None
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_26(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name != self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_27(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is  None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_28(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos >= self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_29(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference or self.end is not None and begin_pos > self.end:
                        self._finished = True
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_30(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = False
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    def xǁTabixFileIterǁ__next____mutmut_31(self) -> str:
        """Return the next record."""
        if self._finished:  # pragma: no cover
            raise StopIteration

        # Load chunks until we find a matching line or run out
        while True:
            # Check if we have lines in current batch
            if self._current_line_idx >= len(self._current_lines):
                # Need to load next chunk
                if not self._load_next_chunk():
                    self._finished = True
                    raise StopIteration

            # Check remaining lines in current batch
            while self._current_line_idx < len(self._current_lines):
                line = self._current_lines[self._current_line_idx]
                self._current_line_idx += 1

                # Skip header/comment lines
                if line.startswith("#"):  # pragma: no cover
                    continue

                # Check if line matches our region
                if self._line_matches_region(line):
                    return line

                # Check if we've gone past our region
                try:
                    seq_name, begin_pos, _ = self._parse_line_position(line)

                    # If we're past our reference sequence, we're done
                    # Use the index order, not lexical order
                    sequence_names = list(self.index.indices.keys())
                    if seq_name in sequence_names and self.reference in sequence_names:
                        seq_idx = sequence_names.index(seq_name)
                        ref_idx = sequence_names.index(self.reference)
                        if seq_idx > ref_idx:  # pragma: no cover
                            self._finished = True
                            raise StopIteration

                    # If we're past our end position, we're done
                    if seq_name == self.reference and self.end is not None and begin_pos > self.end:
                        self._finished = None
                        raise StopIteration

                except (ValueError, IndexError):  # pragma: no cover
                    # Skip malformed lines
                    continue

    xǁTabixFileIterǁ__next____mutmut_mutants = {
    'xǁTabixFileIterǁ__next____mutmut_1': xǁTabixFileIterǁ__next____mutmut_1, 
        'xǁTabixFileIterǁ__next____mutmut_2': xǁTabixFileIterǁ__next____mutmut_2, 
        'xǁTabixFileIterǁ__next____mutmut_3': xǁTabixFileIterǁ__next____mutmut_3, 
        'xǁTabixFileIterǁ__next____mutmut_4': xǁTabixFileIterǁ__next____mutmut_4, 
        'xǁTabixFileIterǁ__next____mutmut_5': xǁTabixFileIterǁ__next____mutmut_5, 
        'xǁTabixFileIterǁ__next____mutmut_6': xǁTabixFileIterǁ__next____mutmut_6, 
        'xǁTabixFileIterǁ__next____mutmut_7': xǁTabixFileIterǁ__next____mutmut_7, 
        'xǁTabixFileIterǁ__next____mutmut_8': xǁTabixFileIterǁ__next____mutmut_8, 
        'xǁTabixFileIterǁ__next____mutmut_9': xǁTabixFileIterǁ__next____mutmut_9, 
        'xǁTabixFileIterǁ__next____mutmut_10': xǁTabixFileIterǁ__next____mutmut_10, 
        'xǁTabixFileIterǁ__next____mutmut_11': xǁTabixFileIterǁ__next____mutmut_11, 
        'xǁTabixFileIterǁ__next____mutmut_12': xǁTabixFileIterǁ__next____mutmut_12, 
        'xǁTabixFileIterǁ__next____mutmut_13': xǁTabixFileIterǁ__next____mutmut_13, 
        'xǁTabixFileIterǁ__next____mutmut_14': xǁTabixFileIterǁ__next____mutmut_14, 
        'xǁTabixFileIterǁ__next____mutmut_15': xǁTabixFileIterǁ__next____mutmut_15, 
        'xǁTabixFileIterǁ__next____mutmut_16': xǁTabixFileIterǁ__next____mutmut_16, 
        'xǁTabixFileIterǁ__next____mutmut_17': xǁTabixFileIterǁ__next____mutmut_17, 
        'xǁTabixFileIterǁ__next____mutmut_18': xǁTabixFileIterǁ__next____mutmut_18, 
        'xǁTabixFileIterǁ__next____mutmut_19': xǁTabixFileIterǁ__next____mutmut_19, 
        'xǁTabixFileIterǁ__next____mutmut_20': xǁTabixFileIterǁ__next____mutmut_20, 
        'xǁTabixFileIterǁ__next____mutmut_21': xǁTabixFileIterǁ__next____mutmut_21, 
        'xǁTabixFileIterǁ__next____mutmut_22': xǁTabixFileIterǁ__next____mutmut_22, 
        'xǁTabixFileIterǁ__next____mutmut_23': xǁTabixFileIterǁ__next____mutmut_23, 
        'xǁTabixFileIterǁ__next____mutmut_24': xǁTabixFileIterǁ__next____mutmut_24, 
        'xǁTabixFileIterǁ__next____mutmut_25': xǁTabixFileIterǁ__next____mutmut_25, 
        'xǁTabixFileIterǁ__next____mutmut_26': xǁTabixFileIterǁ__next____mutmut_26, 
        'xǁTabixFileIterǁ__next____mutmut_27': xǁTabixFileIterǁ__next____mutmut_27, 
        'xǁTabixFileIterǁ__next____mutmut_28': xǁTabixFileIterǁ__next____mutmut_28, 
        'xǁTabixFileIterǁ__next____mutmut_29': xǁTabixFileIterǁ__next____mutmut_29, 
        'xǁTabixFileIterǁ__next____mutmut_30': xǁTabixFileIterǁ__next____mutmut_30, 
        'xǁTabixFileIterǁ__next____mutmut_31': xǁTabixFileIterǁ__next____mutmut_31
    }

    def __next__(self, *args, **kwargs):
        return _mutmut_trampoline(object.__getattribute__(self, "xǁTabixFileIterǁ__next____mutmut_orig"), object.__getattribute__(self, "xǁTabixFileIterǁ__next____mutmut_mutants"), *args, **kwargs) 

    __next__.__signature__ = _mutmut_signature(xǁTabixFileIterǁ__next____mutmut_orig)
    xǁTabixFileIterǁ__next____mutmut_orig.__name__ = 'xǁTabixFileIterǁ__next__'



            # Processed all lines in current batch, loop to load next chunk
