--- src/record/ids.rs
+++ replace <impl crate::variant::record::Ids for Ids<'_>>::iter -> Box<dyn Iterator<Item =&str>+'_> with Box::new(Default::default())
@@ -21,23 +21,17 @@
         self.0.is_empty()
     }
 
     fn len(&self) -> usize {
         self.iter().count()
     }
 
     fn iter(&self) -> Box<dyn Iterator<Item = &str> + '_> {
-        const DELIMITER: char = ';';
-
-        if self.is_empty() {
-            return Box::new(iter::empty());
-        }
-
-        Box::new(self.0.split(DELIMITER))
+        Box::new(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
     use crate::variant::record::Ids as _;
 
