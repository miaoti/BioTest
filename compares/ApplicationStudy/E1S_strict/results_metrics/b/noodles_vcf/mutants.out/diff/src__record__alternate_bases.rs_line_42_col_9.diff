--- src/record/alternate_bases.rs
+++ replace <impl crate::variant::record::AlternateBases for AlternateBases<'_>>::iter -> Box<dyn Iterator<Item = io::Result<&str>>+'_> with Box::new(Default::default())
@@ -34,21 +34,17 @@
         if self.is_empty() {
             0
         } else {
             self.iter().count()
         }
     }
 
     fn iter(&self) -> Box<dyn Iterator<Item = io::Result<&str>> + '_> {
-        if self.is_empty() {
-            Box::new(iter::empty())
-        } else {
-            Box::new(self.0.split(DELIMITER).map(Ok))
-        }
+        Box::new(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
 
     #[test]
