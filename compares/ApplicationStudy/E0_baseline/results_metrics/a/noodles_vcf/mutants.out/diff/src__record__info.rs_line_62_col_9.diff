--- src/record/info.rs
+++ replace <impl crate::variant::record::Info for Info<'_>>::is_empty -> bool with true
@@ -54,17 +54,17 @@
 impl AsRef<str> for Info<'_> {
     fn as_ref(&self) -> &str {
         self.0
     }
 }
 
 impl crate::variant::record::Info for Info<'_> {
     fn is_empty(&self) -> bool {
-        self.0.is_empty()
+        true /* ~ changed by cargo-mutants ~ */
     }
 
     fn len(&self) -> usize {
         const DELIMITER: u8 = b';';
 
         if self.is_empty() {
             0
         } else {
