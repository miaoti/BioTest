--- src/record/samples.rs
+++ replace <impl crate::variant::record::Samples for Samples<'_>>::column_names -> Box<dyn Iterator<Item = io::Result<&'a str>>+'a> with Box::new(Default::default())
@@ -89,17 +89,17 @@
     fn len(&self) -> usize {
         self.iter().count()
     }
 
     fn column_names<'a, 'h: 'a>(
         &'a self,
         _: &'h Header,
     ) -> Box<dyn Iterator<Item = io::Result<&'a str>> + 'a> {
-        Box::new(self.keys().iter().map(Ok))
+        Box::new(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     fn select<'a, 'h: 'a>(
         &'a self,
         _: &'h Header,
         column_name: &str,
     ) -> Option<io::Result<Box<dyn crate::variant::record::samples::Series + 'a>>> {
         self.select(column_name)
